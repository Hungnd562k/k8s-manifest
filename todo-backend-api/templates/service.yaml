apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-backend-api.fullname" . }}
  labels:
    {{- include "todo-backend-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-backend-api.selectorLabels" . | nindent 4 }}
